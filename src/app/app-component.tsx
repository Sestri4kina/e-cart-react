import * as React from "react";
import {History} from 'history';

//import {Route, Router, Switch} from 'react-router-dom';
import {ProductList} from './containers';

export const App: React.StatelessComponent<{}> = () => (<ProductList/>)
